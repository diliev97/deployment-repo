apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps-prod
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/diliev97/deployment-repo
        revision: master
        directories:
          - path: apps/*/base
  template:
    metadata:
      name: "{{path[1]}}-prod"
      labels:
        app: "{{path[1]}}"
        env: prod
      finalizers:
        - resources-finalizer.argocd.argoproj.io
      annotations:
        argocd-image-updater.argoproj.io/image-list: "image=ghcr.io/diliev97/{{path[1]}}:*-prod"
        argocd-image-updater.argoproj.io/write-back-method: git
        argocd-image-updater.argoproj.io/helm.write-back-target: "../overlays/prod/values.yaml"
        notifications.argoproj.io/on-image-update: "ms-teams"
        notifications.argoproj.io/on-sync-failed: "ms-teams"
        notifications.argoproj.io/on-sync-succeeded: "ms-teams"
        notifications.argoproj.io/on-health-degraded: "ms-teams"
        notifications.argoproj.io/on-health-healthy: "ms-teams"
    spec:
      project: default
      sources:
        - repoURL: https://github.com/diliev97/deployment-repo
          targetRevision: master
          path: "apps/{{path[1]}}/base"
          helm:
            valueFiles:
              - "../overlays/prod/values.yaml"
      destination:
        server: https://kubernetes.default.svc
        namespace: prod
      syncPolicy:
        automated:
          prune: true
          selfHeal: false
          allowEmpty: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 3
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 5m